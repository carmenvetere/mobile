clean_basement:
  alias: Clean Basement
  sequence:
  - service: vacuum.send_command
    metadata: {}
    data:
      command: app_segment_clean
      params:
      - segments:
        - 16
        - 17
        - 18
        - 20
        - 21
        repeat: 1
    target:
      entity_id: vacuum.roborock_s8
  description: ''
  icon: mdi:vacuum-outline
reset_vacuum_timer:
  alias: Reset Vacuum Timer
  sequence:
  - service: input_datetime.set_datetime
    target:
      entity_id: input_datetime.last_pool_vacuum
    data:
      datetime: '{{ now().strftime(''%Y-%m-%d %H:%M:%S'') }}'
  description: ''
  icon: mdi:pool
power_outage_cooling:
  alias: 'Power Outage: Cooling'
  sequence:
  - action: input_select.select_option
    data:
      option: Cool
    target:
      entity_id:
      - input_select.living_thermostat_last_hvac_mode
      - input_select.basement_thermostat_last_hvac_mode
      - input_select.bedroom_thermostat_last_hvac_mode
      - input_select.office_thermostat_last_hvac_mode
  - metadata: {}
    data:
      hvac_mode: 'off'
    target:
      entity_id:
      - climate.office_thermostat
      - climate.living_room_thermostat
      - climate.basement_thermostat
    action: climate.set_hvac_mode
  - metadata: {}
    data:
      temperature: 75
      hvac_mode: cool
    action: climate.set_temperature
    target:
      entity_id: climate.bedroom_thermostat
  - action: automation.turn_off
    target:
      entity_id:
      - automation.basement_hvac_schedule
      - automation.bedroom_hvac_schedule
      - automation.enhanced_first_floor_climate_manager
      - automation.office_thermostat_schedule_v2
    data:
      stop_actions: true
  description: ''
  icon: mdi:flash-alert
power_outage_heating:
  alias: 'Power Outage: Heating'
  sequence:
  - metadata: {}
    data:
      temperature: 64
    action: climate.set_temperature
    target:
      entity_id: climate.bedroom_thermostat
  - action: climate.set_temperature
    metadata: {}
    data:
      temperature: 62
    target:
      entity_id:
      - climate.living_room_thermostat
      - climate.office_thermostat
  - action: climate.set_temperature
    metadata: {}
    data:
      temperature: 60
    target:
      entity_id: climate.basement_thermostat
  - action: climate.set_preset_mode
    metadata: {}
    data:
      preset_mode: PermanentHold
    target:
      entity_id:
      - climate.basement_thermostat
      - climate.living_room_thermostat
      - climate.master_bedroom_thermostat
      - climate.office_thermostat
  - action: switch.turn_on
    metadata: {}
    data: {}
    target:
      entity_id:
      - switch.office_thermostat_office_emergency_heat
      - switch.living_room_thermostat_living_room_emergency_heat
      - switch.basement_thermostat_basement_emergency_heat
      - switch.master_bedroom_thermostat_master_bedroom_emergency_heat
  - action: automation.turn_off
    target:
      entity_id:
      - automation.basement_hvac_schedule
      - automation.bedroom_hvac_schedule
      - automation.enhanced_first_floor_climate_manager
      - automation.office_thermostat_schedule_v2
    data:
      stop_actions: true
  description: ''
  icon: mdi:flash-alert
pool_music:
  alias: Pool Music
  sequence:
  - service: media_player.play_media
    metadata: {}
    data:
      media_content_type: music
      enqueue: play
      media_content_id: x-sonos-spotify:spotify%3atrack%3a32iLyDlWw7DHowE5TiXZpi?sid=12&flags=8232&sn=9
    target:
      entity_id: media_player.pool
  description: ''
  icon: mdi:music
mop_mudroom:
  alias: Mop Mudroom
  sequence:
  - service: vacuum.set_fan_speed
    metadata: {}
    data:
      fan_speed: 'Off'
    target:
      entity_id: vacuum.roborock_s8
  - service: select.select_option
    metadata: {}
    data:
      option: intense
    target:
      entity_id:
      - select.roborock_s8_mop_intensity
  - service: select.select_option
    metadata: {}
    data:
      option: deep_plus
    target:
      entity_id: select.roborock_s8_mop_mode
  - service: vacuum.send_command
    metadata: {}
    data:
      command: app_segment_clean
      params:
      - segments:
        - 16
        - 20
        repeat: 2
    target:
      entity_id: vacuum.roborock_s8
    enabled: true
  description: ''
reset_lower_floors_filter_runtime:
  alias: reset_lower_floors_filter_runtime
  sequence:
  - target:
      entity_id: input_datetime.lower_floors_air_filter_last_reset
    data:
      datetime: '{{ now().strftime(''%Y-%m-%d %H:%M:%S'') }}'
    action: input_datetime.set_datetime
  - action: input_number.set_value
    metadata: {}
    target:
      entity_id: input_number.lower_floors_hvac_filter_hours
    data:
      value: 0
  description: ''
reset_upper_floors_filter_runtime:
  alias: reset_upper_floors_filter_runtime
  sequence:
  - target:
      entity_id: input_datetime.upper_floor_air_filter_last_reset
    data:
      datetime: '{{ now().strftime(''%Y-%m-%d %H:%M:%S'') }}'
    action: input_datetime.set_datetime
  - action: input_number.set_value
    metadata: {}
    target:
      entity_id: input_number.upper_floor_hvac_filter_hours
    data:
      value: 0
  description: ''
power_returns:
  alias: Power Returns
  sequence:
  - action: climate.set_preset_mode
    metadata: {}
    data:
      preset_mode: NoHold
    target:
      entity_id:
      - climate.basement_thermostat
      - climate.living_room_thermostat
      - climate.master_bedroom_thermostat
      - climate.office_thermostat
  - action: switch.turn_off
    metadata: {}
    data: {}
    target:
      entity_id:
      - switch.office_thermostat_office_emergency_heat
      - switch.basement_thermostat_basement_emergency_heat
      - switch.living_room_thermostat_living_room_emergency_heat
      - switch.master_bedroom_thermostat_master_bedroom_emergency_heat
  - action: automation.turn_on
    target:
      entity_id:
      - automation.basement_hvac_schedule
      - automation.enhanced_first_floor_climate_manager
      - automation.bedroom_hvac_schedule
      - automation.office_thermostat_schedule_v2
    data: {}
  - action: automation.trigger
    target:
      entity_id:
      - automation.basement_hvac_schedule
      - automation.bedroom_hvac_schedule
      - automation.enhanced_first_floor_climate_manager
      - automation.office_thermostat_schedule_v2
    data:
      skip_condition: true
  description: ''
  icon: mdi:lightning-bolt-outline
reset_hot_water_last_flushed_date:
  sequence:
  - target:
      entity_id: input_datetime.water_heater_last_flushed
    data:
      datetime: '{{ now().strftime(''%Y-%m-%d %H:%M:%S'') }}'
    action: input_datetime.set_datetime
  alias: Reset Hot Water Last Flushed Date
  description: ''
  icon: mdi:water-boiler-alert
reset_dryer_vent_last_cleaned_date:
  sequence:
  - target:
      entity_id: input_datetime.dryer_vent_last_cleaned
    data:
      datetime: '{{ now().strftime(''%Y-%m-%d %H:%M:%S'') }}'
    action: input_datetime.set_datetime
  alias: Reset Dryer Vent Last Cleaned Date
  description: ''
  icon: mdi:tumble-dryer
push_live_activity:
  alias: Push Live Activity
  description: >-
    Reusable iOS Live Activity delivery (#179). Fans out one mobile_app notify
    per recipient by audience (both|carmen|brian), injecting tag + live_update
    into the payload dict. Per-activity automations call this instead of
    repeating notify blocks.
  mode: parallel
  max: 25
  icon: mdi:cellphone-message
  fields:
    tag:
      description: Unique activity tag; same tag updates the card in place.
    title:
      description: Static Live Activity title.
    message:
      description: Body line (may change between updates).
    payload:
      description: Extra notify data keys (progress, chronometer, when, icons, ...).
    audience:
      description: both | carmen | brian (default both).
  variables:
    audience: "{{ audience | default('both') }}"
    payload: "{{ payload | default({}) }}"
    recipients: >-
      {{ ['notify.mobile_app_carmens_iphone'] if audience == 'carmen'
         else ['notify.mobile_app_brians_iphone_15_pro'] if audience == 'brian'
         else ['notify.mobile_app_carmens_iphone', 'notify.mobile_app_brians_iphone_15_pro'] }}
  sequence:
  - repeat:
      for_each: "{{ recipients }}"
      sequence:
      - action: "{{ repeat.item }}"
        data:
          title: "{{ title }}"
          message: "{{ message }}"
          # Dark background + light text as defaults (a caller's payload can
          # override); tag + live_update always win. These are documented iOS
          # Live Activity fields.
          data: >-
            {{ {'background_color': '#14161A', 'text_color': '#FFFFFF'}
               | combine(payload)
               | combine({'tag': tag, 'live_update': true}) }}
shades_latch_manual:
  alias: Shades - Latch Room to Manual
  description: >-
    v2.4.1: the one place a room's input_boolean.shades_manual_* latch gets
    set by the Pico remotes. Those automations drive the covers themselves, so
    their cover events carry a parent_id and the Manual Control Detector
    deliberately ignores them - they latch explicitly, and before v2.4.1 they
    did it unconditionally at any hour and in any room state.
    A manual latch only means something while the Shade Scheduler is actually
    managing the room, so it is refused in two cases:
    (1) outside the scheduler's own active window (07:29-20:01). A press
    before the morning open latched the room out of the very open it was
    waiting for, so the shades never took an adaptive position all day and it
    read as "the overnight reset didn't happen". After 20:01 the scheduler is
    done for the day and the latch would only sit there until 03:00.
    (2) while the room is paused (Overnight Guests for the guest rooms, Dinner
    Party for the First Floor). A paused room is already skipped end-to-end,
    so latching it does nothing at the time and leaves a latch that outlives
    the pause - which is why a guest room stayed out of adaptive after
    Overnight Guests was switched off.
    A refused latch is a silent no-op: the condition ends this script only,
    never the calling automation, so the shades still move.
  mode: parallel
  max: 10
  icon: mdi:hand-back-right
  fields:
    latch:
      description: The room's input_boolean.shades_manual_* entity.
    pause:
      description: >-
        The room's pause helper in the scheduler's rooms table
        (input_boolean.guest_mode / .dinner_party). Omit for a room with none.
  variables:
    pause: "{{ pause | default('') }}"
  sequence:
  - condition: time
    after: '07:29:00'
    before: '20:01:00'
  - condition: template
    value_template: "{{ pause == '' or not is_state(pause, 'on') }}"
  - action: input_boolean.turn_on
    target:
      entity_id: "{{ latch }}"
clear_live_activity:
  alias: Clear Live Activity
  description: >-
    Clears an iOS Live Activity by tag for the chosen audience (#179).
  mode: parallel
  max: 25
  icon: mdi:cellphone-remove
  fields:
    tag:
      description: Activity tag to clear.
    audience:
      description: both | carmen | brian (default both).
  variables:
    audience: "{{ audience | default('both') }}"
    recipients: >-
      {{ ['notify.mobile_app_carmens_iphone'] if audience == 'carmen'
         else ['notify.mobile_app_brians_iphone_15_pro'] if audience == 'brian'
         else ['notify.mobile_app_carmens_iphone', 'notify.mobile_app_brians_iphone_15_pro'] }}
  sequence:
  - repeat:
      for_each: "{{ recipients }}"
      sequence:
      - action: "{{ repeat.item }}"
        data:
          message: clear_notification
          data:
            tag: "{{ tag }}"
toggle_water_heater_away_mode:
  alias: Toggle Water-Heater Away Mode
  sequence:
  - action: water_heater.set_away_mode
    target:
      entity_id: water_heater.smithy_2
    data:
      away_mode: "{% if state_attr('water_heater.smithy_2', 'away_mode') == 'on' %}\n
        \ off\n{% else %}\n  on\n{% endif %}\n"
  mode: single
  description: ''
alarm_keypad_disarm:
  alias: Alarm Keypad Disarm
  sequence:
  - action: alarm_control_panel.alarm_disarm
    target:
      entity_id: alarm_control_panel.alarmo
    data:
      code: '{{ states(''input_text.alarm_code_input'') }}'
  - action: input_text.set_value
    target:
      entity_id: input_text.alarm_code_input
    data:
      value: ''
  description: ''
sprinkler_pause_briefly:
  alias: Sprinkler Pause Briefly
  description: Pauses the running Rachio schedule for 10 minutes (enough to take
    the dog out) and starts timer.sprinkler_pause so the dashboard can show the
    Paused state and countdown. Rachio resumes on its own when the pause window
    expires; "Sprinklers - Resume After Pause Timer" is the backstop. Targets the
    controller entity (switch.rachio_c3e715_standby), same one the outage
    automations use. Known limitation (accepted by design) - pause_watering
    pauses the running SCHEDULE; a manual zone run started from a switch may not
    resume by itself, which the pop-up's paused note calls out.
  icon: mdi:pause
  mode: single
  sequence:
  - action: rachio.pause_watering
    data:
      duration: 10
    target:
      entity_id: switch.rachio_c3e715_standby
  - action: timer.start
    data:
      duration: "00:10:00"
    target:
      entity_id: timer.sprinkler_pause
sprinkler_resume_now:
  alias: Sprinkler Resume Now
  description: Resumes the paused Rachio schedule immediately and cancels the
    pause timer so the pop-up returns to Watering now.
  icon: mdi:play
  mode: single
  sequence:
  - action: rachio.resume_watering
    target:
      entity_id: switch.rachio_c3e715_standby
  - action: timer.cancel
    target:
      entity_id: timer.sprinkler_pause
sprinkler_stop_all:
  alias: Sprinkler Stop All
  description: Stops watering entirely - turns off all seven zone switches (the
    individual zones, not the group, so a partially-running schedule is fully
    stopped) and cancels the pause timer. The header chip disappears once the
    zone group reports off.
  icon: mdi:stop
  mode: single
  sequence:
  - action: switch.turn_off
    target:
      entity_id:
      - switch.front_180s
      - switch.front_and_side_90s
      - switch.pool_180s
      - switch.pool_90s
      - switch.pool_shrubs
      - switch.firepit_shrubs
      - switch.retaining_wall_drip
  - action: timer.cancel
    target:
      entity_id: timer.sprinkler_pause
music_toggle_group:
  alias: "Music: join or leave the controlled speaker's group"
  icon: mdi:speaker-multiple
  mode: queued
  fields:
    entity:
      description: Sonos media_player to add to / remove from the controlled group
      example: media_player.pool
  sequence:
    - variables:
        leader: "{{ states('sensor.music_controlled_player') }}"
        members: "{{ state_attr('sensor.music_controlled_player', 'group_members') or [leader] }}"
    - if:
        - condition: template
          value_template: "{{ entity in members }}"
      then:
        - action: media_player.unjoin
          target:
            entity_id: "{{ entity }}"
      else:
        - action: media_player.join
          target:
            entity_id: "{{ leader }}"
          data:
            group_members:
              - "{{ entity }}"
music_play_item:
  alias: "Music: play an item on the controlled speaker"
  icon: mdi:play
  mode: queued
  fields:
    media_content_id: { description: Sonos id or Music Assistant uri }
    media_content_type: { description: playlist / music / radio / album }
    source: { description: "library (MA uri) or recent (Sonos id)", example: library }
  sequence:
    # Same routing the wall panel uses (automation.basement_panel_play)
    - event: esphome.basement_panel_play
      event_data:
        entity_id: "{{ states('sensor.music_controlled_player') }}"
        media_content_id: "{{ media_content_id }}"
        media_content_type: "{{ media_content_type }}"
        source: "{{ source | default('recent') }}"
music_browse:
  alias: "Music: browse the Music Assistant library"
  icon: mdi:music-box-multiple
  mode: restart
  fields:
    media_content_id: { description: "playlist / radio / album / artist / favorite", example: playlist }
  sequence:
    # The wall panel package answers into sensor.basement_panel_music_browse
    - event: esphome.basement_panel_browse
      event_data:
        entity_id: "{{ states('sensor.music_controlled_player') }}"
        media_content_id: "{{ media_content_id }}"
        media_content_type: "{{ media_content_id }}"

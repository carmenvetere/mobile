# Template for secrets.yaml. Copy to secrets.yaml and fill in real values.
# secrets.yaml itself is git-ignored and must never be committed.

recorder_db_url: "mysql://USER:PASSWORD@core-mariadb/homeassistant?charset=utf8mb4"
alarm_code: "0000"
